{
  "name": "pi-hermes-obsidian-memory",
  "version": "0.9.5",
  "description": "Persistent memory, session search, and secret scanning for Pi. Token-aware policy-only memory, SQLite FTS5 search, auto-consolidation, procedural skills. Inspired by pi-hermes-memory.",
  "type": "module",
  "main": "src/index.ts",
  "files": [
    "src",
    "scripts",
    "README.md",
    "LICENSE",
    "docs/PUBLISHING.md",
    "docs/ROADMAP.md",
    "SECURITY.md",
    "CHANGELOG.md"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "node scripts/ensure-dev.mjs check && tsc --noEmit",
    "check:min-sdk": "node scripts/ensure-dev.mjs check && node scripts/check-min-sdk.mjs",
    "test": "node scripts/ensure-dev.mjs test && node scripts/run-tests.mjs"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "memory",
    "persistent-memory",
    "session-search",
    "sqlite",
    "fts5",
    "skills",
    "learning-loop",
    "agent",
    "hermes",
    "secret-scanning",
    "content-scanner",
    "correction-detection",
    "auto-consolidation",
    "memory-aging",
    "context-fencing",
    "two-tier-memory"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/the-matt-moo/pi-hermes-obsidian-memory"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.80.1",
    "@earendil-works/pi-coding-agent": ">=0.80.1"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.2",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@types/better-sqlite3": "^7.6.13",
    "tsx": "^4.21.0",
    "typebox": "^1.1.33",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@earendil-works/pi-tui": "^0.80.2",
    "better-sqlite3": "^12.9.0",
    "strip-ansi": "7.2.0"
  }
}
